# A `Layout` object

A `Layout` object defines how to place the plots.

## Usage

``` r
LayoutProto(
  data = NULL,
  titles = layout_title(),
  schemes = Schemes(),
  theme = theme(),
  name = NA_character_
)
```
