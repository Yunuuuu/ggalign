# Create ggplot object with layout panel data

**\[deprecated\]**

This function was deprecated because we realised that it's a special
case of the
[`ggalign()`](https://yunuuuu.github.io/ggalign/reference/ggalign.md)
function. Please use `ggalign(data = NULL)` instead.

## Usage

``` r
align_panel(...)

ggpanel(...)
```
